import { AfterContentInit, ElementRef, OnDestroy, Renderer2, ViewContainerRef } from '@angular/core';
/**
 * @description
 *
 * Este componente é utilizado em conjunto thf-list.
 */
export declare class ThfListItemComponent implements AfterContentInit, OnDestroy {
    /** Parâmetro que recebe um titulo a ser renderizado no item. */
    title: string;
    /** Índice do for utilizado para controles internos do componente. */
    index: number;
    /** Lista de ações a ser executadas no item. Ao passar até duas ações é exibido apenas botões,
     * se passar de duas ações é exibido um menu com todas as opções.
     */
    actions: Array<any>;
    actionsIconElement: ElementRef;
    detailsContent: any;
    content: any;
    _parentRef: any;
    collapsed: boolean;
    showAction: boolean;
    showLink: boolean;
    showDetail: boolean;
    showContent: boolean;
    private clickListener;
    constructor(viewRef: ViewContainerRef, renderer: Renderer2);
    ngAfterContentInit(): void;
    ngOnDestroy(): void;
    readonly parentRef: any;
    private checkClickedOutActionsIcon;
    private closeActions;
    private closeListActionsWhenClickedOut;
}
